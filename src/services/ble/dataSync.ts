/**
 * Device data synchronisation – syncs device session logs from the device
 * to Firebase when a connection is established.
 * Placeholder for future device-to-cloud sync logic.
 */

export async function syncDeviceData(_deviceId: string): Promise<void> {
  // 1. Read buffered session data from device
  // 2. Upload to Firestore
  // 3. Clear device buffer
  console.log('Device data sync – not yet implemented');
}
